import React from 'react'

const Input = ({label}) => {
  return (
    <div>
      <label>{label}</label>
    <input className={StyleSheet.input}/>
    </div>
    
      
    
  )
}

export default Input
